#include <stdio.h>
/**
 * main - program that prints the lowercase alphabet in reverse
 * Return: 0 (Success)
 */

int main(void)
{
	char f;

	f = 'z';
	while
		(f >= 'a') {
			putchar(f);
			f--;
		}
	putchar('\n');
	return (0);
}
