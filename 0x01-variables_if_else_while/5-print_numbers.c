#include <stdio.h>
/**
 * main - program that prints all single digit numbers of base
 * Return: 0 (Success)
 */

int main(void)
{
	int p;

	for (p = 0; p < 10; p++)
		printf("%d", p);
	putchar('\n');
	return (0);
}
